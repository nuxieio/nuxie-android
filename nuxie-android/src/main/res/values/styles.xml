<resources>
  <!--
    Flow presentation theme.

    Keep dependencies minimal by using platform themes (no AppCompat requirement).
  -->
  <style name="Nuxie.Flow.ActivityTheme" parent="@android:style/Theme.Translucent.NoTitleBar.Fullscreen">
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:colorBackgroundCacheHint">@null</item>
  </style>
</resources>

